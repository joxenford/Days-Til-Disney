The countdown itself. Sits inside a ParkPanel at hero size on Home.

```jsx
<CountdownNumeral value={45} unit="days" />
<CountdownNumeral value={3} unit="of 7 days" size="screen" />
```

Never centre it, never outline it, never animate the digits individually.
