36px rounded-square button used in screen headers.

```jsx
<IconButton glyph="+" tone="loud" label="Add trip" />
<IconButton glyph="•••" label="Settings" />
```

Always paired at the top-right of a screen, or alone at the top-left as back ('‹'). The visual box is 36px; keep 44px of hit area around it.
