Packing progress. Always accompanied by a "12/38" numeral — the bar never carries the number alone.

```jsx
<ProgressBar value={12} total={38} />
<ProgressBar value={12} total={38} tone="gold" height={10} />
```
