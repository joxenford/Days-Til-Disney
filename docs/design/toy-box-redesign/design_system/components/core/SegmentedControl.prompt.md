Two- or three-way choice; used for the theme picker (Light / Dark / System).

```jsx
<SegmentedControl options={['Light','Dark','System']} value="Dark" />
```

Labels are sentence case and short. For longer option sets use chips instead.
