Pill for park names, resorts, dates and filter rows.

```jsx
<Chip tone="onPark">Magic Kingdom</Chip>
<Chip tone="surface" selected>Tokyo Disneyland</Chip>
```

Wrap in a flex row with an 8px gap. Selected chips invert to ink; never use colour to indicate selection.
